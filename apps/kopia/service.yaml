---
apiVersion: v1
kind: Service
metadata:
  annotations:
  labels:
    app.kubernetes.io/instance: kopia
    app.kubernetes.io/name: kopia
  name: kopia
spec:
  ports:
  - name: grpc
    port: 80
    protocol: TCP
    targetPort: grpc
  selector:
    app.kubernetes.io/instance: kopia
    app.kubernetes.io/name: kopia
